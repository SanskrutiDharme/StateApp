const Sub = (test) => {
    return(
        <div>
            <p>This is sub Component</p>
            <p>Substraction = {parseInt(test.a) - parseInt(test.b)}</p>
        </div>
    )
};

export default Sub;
